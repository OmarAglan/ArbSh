#ifndef _CONFIG_H_
#define _CONFIG_H_

#include "shell.h" // Include main header if needed

// Function to load configuration
void load_configuration(info_t *info);

// Maybe add functions to get specific config values later
// const char *get_config_string(const char *key, const char *default_value);
// int get_config_int(const char *key, int default_value);

#endif // _CONFIG_H_